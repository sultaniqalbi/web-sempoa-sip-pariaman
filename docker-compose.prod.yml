version: '3.9'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        - VITE_API_URL=/api/v1
    container_name: sempoa_frontend_prod
    restart: unless-stopped
    networks:
      - sempoa-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sempoa_backend_prod
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-sempoa_prod}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-sempoa_sip}
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - FASTAPI_ENV=production
      - SECRET_KEY=${SECRET_KEY}
      - ESP32_API_KEY=${ESP32_API_KEY:?ESP32_API_KEY harus di-set di .env}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ALLOWED_ORIGINS=https://sempoasippariaman.com,https://www.sempoasippariaman.com
      - GOOGLE_WEBHOOK_URL=https://script.google.com/macros/s/AKfycbxvnOx98V0kyz_ifxqM_Dp_i3m3TPrqdKCo9DBJcubP9qKGG_AM9ewVap0RpUxEVJw4/exec
    volumes:
      - ./backend/app/uploads:/app/app/uploads
      - ./backend/backups:/app/backups
    depends_on:
      - db
      - redis
    restart: unless-stopped
    networks:
      - sempoa-network

  redis:
    image: redis:7-alpine
    container_name: sempoa_redis_prod
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - sempoa-network

  db:
    image: postgres:15-alpine
    container_name: sempoa_db_prod
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-sempoa_prod}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB:-sempoa_sip}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - sempoa-network

  nginx:
    image: nginx:alpine
    container_name: sempoa_nginx_prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
    depends_on:
      - frontend
      - backend
    restart: unless-stopped
    networks:
      - sempoa-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  sempoa-network:
    driver: bridge
